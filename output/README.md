# HTML chunking operations will deposit their .dat file in this directory, and embedding operations will attempt to process all .dat files present in this directory.

   ```
   cd ./php
   php cb-create-embeddings.php
   ```
